recommendations/
    llmfit predictions made before models are running

benchmarks/
    actual measurements from models run on this machine

scripts/capture-recommendations.sh
    regenerate recommendations after llmfit/model-catalog updates

scripts/benchmark-model.sh
    benchmark another running model

scripts/share-benchmarks.sh
    contribute pending measurements upstream
